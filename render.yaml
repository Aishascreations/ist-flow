services:
  - type: web
    name: ist-flow-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGO_URI
        sync: false   # You will paste this in Render dashboard
      - key: PORT
        value: 5000
